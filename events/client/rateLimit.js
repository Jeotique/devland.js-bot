const Bot = require('../../structures/client')
const Discord = require('devland.js')

module.exports = {
    name: 'rateLimit',
    /**
     * 
     * @param {Bot} client 
     */
    run: async (client, info) => {
        return;
        console.log(`${info}`)
    }
}